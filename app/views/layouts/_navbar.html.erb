
<nav class="navbar navbar-default navbar-static-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#" >LazyNomad</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
     
      <%= form_tag(queries_path, :method => "get", class: "navbar-form searchForm navbar-left", id: "search-form") do %>
        <%= text_field_tag "search", nil, class: "input-field", placeholder: "Where do you want to go?", type: "search" %>
         <%= button_to("Search!", queries_path, :method => "get", class: "btn btn-primary") %>
      <% end %>

      <%= form_tag("/near_me", :method => "get", class: "navbar-form navbar-left", id: "retrieveLocation") do %>
        <%= hidden_field_tag(:push_location) %>
        <%= submit_tag("Search Near Me!", class: "btn btn-primary") %>
      <% end %>
    
      <% if user_signed_in? %>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to("Hello #{current_user.username}!", "#") %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to("Edit Profile", edit_user_registration_path) %></li>
            <li><a href="/saved_places" class="saved-places">My Saved Places</a></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to("Log out", destroy_user_session_path, method: :delete) %></li>
          </ul>
        </li>
      </ul> 
      <% else %>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to("Sign Up", new_user_registration_path) %></li>
        <li><%= link_to("Log in", new_user_session_path) %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to("Edit Profile", edit_user_registration_path) %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to("Log out", destroy_user_session_path, method: :delete) %></li>
          </ul>
        </li>
      </ul>
      <% end %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>